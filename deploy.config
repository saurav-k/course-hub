# Course Hub deploy config - reused by deploy.sh and by Claude Code.
# Edit these if the profile / bucket / region ever change.
AWS_PROFILE=aqi-account
AWS_REGION=ap-south-2
BUCKET=aqi-course-hub-149536460784
