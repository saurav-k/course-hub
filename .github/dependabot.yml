version: 2

updates:
  # The workflows pin action major versions. Dependabot opens a pull request
  # when a pinned action moves, so the pipeline never silently rots.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: chore
    labels:
      - dependencies
